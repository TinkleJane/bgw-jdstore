<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid content_container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">B&W
      </a>
    </div>

    <ol class="nav navbar-nav navbar-left">
      <li><%= link_to("Products", products_path) %></li>
      <li><%= link_to('About',about_welcome_index_path)%></li>
    </ol>

    <ol class="nav navbar-nav navbar-right">
      <li>
        <%= link_to carts_path do  %>
        购物车
        <i class="fa fa-shopping-cart"></i>
        (
        <%= current_cart.cart_items.count %>
        )
        <% end %>
      </li>

      <% if !current_user %>
          <li><%= link_to("注册", new_user_registration_path) %></li>
          <li><%= link_to(content_tag(:i, '登入', class: 'fa fa-sign-in'), new_user_session_path)%></li>
      <% else %>
          <li><%= link_to(content_tag(:i, "登出", class: "fa fa-sign-out"), destroy_user_session_path, :method => :delete)%></li>
      <% end %>

      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Dear <%= current_user.email.split("@")[0] %> <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to("我的订单", account_orders_path)%></li>
          <li><%= link_to("我的藏品",account_collected_products_path)%></li>
          <li role="separator" class="divider"></li>
          <% if current_user.admin?%>
              <li><%= link_to("Admin 选单", admin_products_path)%></li>
          <% end %>
        </ul>
      </li>
    </ol>
  </div>
  <!-- /.navbar-collapse -->
</div>
<!-- /.container-fluid -->



</nav>
