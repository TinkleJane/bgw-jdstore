<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid content_container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">B&W
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to("Products", products_path) %>
        </li>
        <li class="mob-nav-account">
          <%= link_to('About',about_welcome_index_path)%>
        </a>
      </li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li>
        <%= link_to carts_path do  %>
        <i class="fa fa-shopping-cart"></i>
        (
        <%= current_cart.cart_items.count %>
        )
        <% end %>
      </li>
      <% if !current_user %>
      <li><%= link_to("注册", new_user_registration_path) %>
      </li>
      <li><%= link_to(content_tag(:i, '登入', class: 'fa fa-sign-in'), new_user_session_path)%></li>
    <% else %>
    <li id="hiuser">Hi!, <%= current_user.email.split("@")[0] %> </li>
        <% if current_user.admin?%>
        <li>
          <%= link_to("Admin 选单", admin_products_path)%>
        </li>
        <% end %>
        <li>
          <%= link_to("我的订单", account_orders_path)%>
        </li>
        <li>
          <%= link_to(content_tag(:i, "登出", class: "fa fa-sign-out"), destroy_user_session_path, :method => :delete)%>
        </li>
      <% end %>
    </ul>
  </div>
  <!-- /.navbar-collapse -->
</div>
<!-- /.container-fluid -->
</nav>
